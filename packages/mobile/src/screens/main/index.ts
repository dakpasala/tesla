// packages/mobile/src/screens/main/index.ts

// Main app screens - export barrel file

// Centralizes all main screen exports for clean imports throughout the app.
// Add new main screens here to make them available via a single import path.

export { default as MapScreen } from './MapScreen';
export { default as FavoritesScreen } from './FavoritesScreen';
export { default as SettingsScreen } from './SettingsScreen';
export { default as RewardsScreen } from './RewardsScreen';
